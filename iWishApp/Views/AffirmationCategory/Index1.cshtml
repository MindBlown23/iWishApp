@model List<iWishApp.Models.AffirmationsCategory>

<h1>All Event Categories</h1>

@if(Model.Count == 0)
{
    <p>No Affirmations Categories yet!</p>
}
else
{
    <table class="table">
        <tr>
            <th>Id
             <th> Category Name</th>
        </tr>
        @foreach (AffirmationsCategory category in Model)
        {
            <tr>
                <td>@category.Id</td>  
                <td>@category.Name</td>
            </tr>
        }
    </table>
}